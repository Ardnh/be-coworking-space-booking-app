meta {
  name: delete resource type
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/resource-type/?id
  body: json
  auth: inherit
}

params:query {
  id: 
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

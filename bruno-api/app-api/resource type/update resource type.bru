meta {
  name: update resource type
  type: http
  seq: 4
}

put {
  url: {{base_url}}/resource-type/?id
  body: json
  auth: inherit
}

params:query {
  id: 
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
